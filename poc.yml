pipelines:
  - name: my_first_pipeline
    steps:
      - name: myFirstStep
        type: Bash
        execution:
          onExecute:
            - echo "Hello world!"
            - echo "============"
